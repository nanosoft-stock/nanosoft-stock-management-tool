import 'package:flutter/material.dart';

class ArchiveProductScreen extends StatelessWidget {
  const ArchiveProductScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
