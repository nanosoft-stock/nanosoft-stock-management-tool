import 'package:flutter/material.dart';

class PrintIdView extends StatelessWidget {
  const PrintIdView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
