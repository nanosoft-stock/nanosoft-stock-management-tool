import 'package:flutter/material.dart';

class ArchiveStockScreen extends StatelessWidget {
  const ArchiveStockScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
